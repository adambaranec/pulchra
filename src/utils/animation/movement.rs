use web_sys::*;
use wasm_bindgen::closure::Closure;
use wasm_bindgen::JsCast;
use wasm_bindgen::prelude::wasm_bindgen;
pub fn animate_uniform(uniform: &WebGlUniformLocation, what_to_change: u8, new: f32){
let window = web_sys::window();
}
#[wasm_bindgen]
pub fn move_model(x: f32, y: f32, z: f32){
let window = web_sys::window().unwrap();
let recursion = Closure::once(move || {});
}

pub fn rotate(degree: &u16){

}